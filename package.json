{
  "name": "firefox-statement-downloader-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Firefox extension that bulk-downloads statements and related financial documents from supported institutions.",
  "scripts": {
    "build": "mkdir -p dist && rm -f dist/firefox-statement-downloader-extension.zip && zip -X dist/firefox-statement-downloader-extension.zip manifest.json background.js content.js panel.css",
    "clean": "rm -rf dist"
  }
}
